# use -i to in-place alter the file content matched
sed -i 's/oldStr/newStr/g' filename

# use '/match_string/{s/oldStr/newStr/g}'
sed -i '/Google/{s/^/#&/}' filename

#delete n characters at the end of each line
sed 's/\(.\)\{n\}$//' file 


